user wallet system is a basic system to try out
	import of diff modules, with custom exception, each having its own interface, and  
	different modules interact only with what it should see, nothing more
	also, database to use sorted containers of python to query transactions